apiVersion: v1
kind: Service
metadata:
  name: {{ include "social-ledger.fullname" . }}-frontend
  labels:
    {{- include "social-ledger.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.frontend.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "social-ledger.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
